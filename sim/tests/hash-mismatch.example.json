{
  "$schema": "./test-case.schema.json",
  "test_id": "hash-mismatch",
  "description": "Reader peers should deterministically reject the injected hash mismatch according to the fault plan.",
  "category": "negative",
  "topology": "sim/topologies/hash-mismatch.example.json",
  "fixture_set": "fixtures/object-sets/hash-mismatch",
  "execution_mode": "single-process",
  "expected_result": "fail",
  "expected_outcomes": [
    "object-rejected-hash-mismatch",
    "no-invalid-index-write"
  ],
  "assertions": [
    {
      "assertion_id": "fault-plan-present",
      "description": "The generated report exposes a deterministic non-empty fault plan.",
      "severity": "error"
    },
    {
      "assertion_id": "object-rejected-hash-mismatch",
      "description": "At least one reader rejects the planned hash mismatch injection.",
      "severity": "error"
    }
  ],
  "notes": [
    "Use this to inspect how deterministic seed ordering drives negative hash mismatch injection."
  ]
}
